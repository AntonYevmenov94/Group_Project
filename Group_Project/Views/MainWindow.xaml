<Window x:Class="Group_Project.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Group_Project"
        xmlns:convert="clr-namespace:Group_Project.Converters"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="MainWindow" Height="450" Width="800">
    
    <Grid>
        <!--DataContext устанавливается на ViewModel выбранной страницы, чтобы она сразу унаследовала его
        как свой DataContext-->
        <ContentControl 
            DataContext="{Binding AppPageNavService.ActivePageViewModel}"
            Content="{Binding DataContext.AppPageNavService.ActiveAppPage, 
            RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, 
            Converter={convert:AppPageToViewConverter}}">

        </ContentControl>
    </Grid>
</Window>