<Window x:Class="Group_Project.Views.WindowGenericList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Group_Project.Views"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" 
        Height="400" MinHeight="250"
        Width="400" MinWidth="300">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <GroupBox Grid.Column="0" Header="{Binding ListTitle, FallbackValue=List content}">
            <ListBox 
                Margin="-5"
                ItemsSource="{Binding ListItems}"
                SelectedItem="{Binding SelectedListItem}"
                DisplayMemberPath="Title"
                ScrollViewer.VerticalScrollBarVisibility="Visible">
            </ListBox>
        </GroupBox>

        <StackPanel Grid.Column="1" Margin="0 30 0 0">
            <Button Margin="10" Content="Добавить" Command="{Binding CmdAddListItem}"/>
            <Button Margin="10" Content="Изменить" Command="{Binding CmdEditListItem}"/>
            <Button Margin="10" Content="Удалить" Command="{Binding CmdDeleteListItem}"/>
        </StackPanel>

    </Grid>
</Window>